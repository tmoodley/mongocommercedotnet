@model seoWebApplication.Data.city

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_BaseAdminSingle.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>city</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.idCity)

        <div class="form-group">
            @Html.LabelFor(model => model.idState, "idState", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("idState", String.Empty)
                @Html.ValidationMessageFor(model => model.idState)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.city1, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.city1)
                @Html.ValidationMessageFor(model => model.city1)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
